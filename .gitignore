/.claude/

# Bí mật — `.env.example` là bản mẫu và ĐƯỢC commit; `.env` là bản thật và không bao giờ commit.
.env
.env.*
!.env.example

# Build và dependency
node_modules/
dist/
.vite/
coverage/
playwright-report/
test-results/

# Cache cua tsc. `npm run typecheck` sinh ra no moi lan chay.
*.tsbuildinfo
